return {
  "folke/noice.nvim",
  opts = {
    lsp = {
      signature = {
        -- Disables LSP signature from popping up
        -- automatically, as it got too annoying
        enabled = false,
      },
    },
  },
}
